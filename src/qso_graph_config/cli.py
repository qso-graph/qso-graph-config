"""QSO-Graph Config — meta-package for ham radio MCP servers.

The interactive manager is a pure bash script (qso-graph-config.sh).
This Python package exists solely to pull in MCP server dependencies via pip.
"""
